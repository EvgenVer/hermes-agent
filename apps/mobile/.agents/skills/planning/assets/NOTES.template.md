# NOTES — working notes for the current task
<!-- High churn. Progress, resume points, open threads. Clean out when the task closes
     (run the planning skill's NOTES closeout first). Stable facts → MEMORY.md. -->

## Current task & state

## Decisions taken

## Open threads / next steps

<!-- ───────── EXAMPLE — delete this whole block before use ─────────
## Current task & state
Implementing the weekly-digest grouping (PLAN phase 2). Parser done; grouping-by-decision
half-wired. Resume at `digest.ts:groupByDecision` — speaker-merge case still TODO.

## Decisions taken
- Group by decision, not speaker (per DESCRIPTION).
- Markdown output only for v1; PDF deferred.

## Open threads / next steps
- [ ] Handle transcripts with no `S1:` labels (fallback to single speaker).
- [ ] Add fixture with a multi-decision meeting before closing.
───────────────────────────────────────────────────────────────── -->
